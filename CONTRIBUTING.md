<!-- markdownlint-disable MD033 MD041 -->

# 参与 MaaStellaSora 贡献

星塔旅人作为一款小体量的游戏，能吸引到的开发人员相当有限，所幸经过磕磕碰碰，终于把**基础功能**都完成了。
但是，面对各种用户不同的需求，星塔助手能做到的仍然有限，所以我们非常欢迎各种形式的贡献。

## 贡献方式
作为一款长期人手不足的项目，我们对贡献的方式非常宽容，但是为了保证项目能够长期维护，我们还是希望贡献者能先阅读以下内容：
### **bug修复**
   * bug修复是最简单的贡献，也是最重要的贡献之一。如果你不懂编程，或者懒得写代码，可以通过**提交 issue** 的方式来帮助我们发现 bug
   * 如果你非常清楚星塔助手出现的bug的原因，可以不理会冗长的issue模板，直接通过**简短的文字**说明bug内容
   * 如果你不确定问题出在哪里，可以通过**调试模式**对运行过程进行记录，然后通过issue模板将截图及日志文件附在issue中，这样我们才有可能定位问题所在
   * 如果你会编程且有能力修复bug，可以直接通过**提交PR**的方式来修复bug，只要你能**阐述bug产生的原因**，大概率会被采纳并合并
>如何对运行过程进行记录，以及日志文件的位置（基于MFAAvalonia）：
> 1. 运行截图
>    * 通过左下角的齿轮图标 -> 选项 -> 运行设置 -> 勾选启用保存截图(用于调试) -> 开启显示节点命中截图(用于调试) 开启
>    * 截图文件会保存在软件目录下的`debug/vision`文件夹中
>    * 可以浏览内容，只取出现bug位置的附近的截图进行上传
>    * 注意！不截图时记得关闭该功能，否则会占用大量磁盘空间，且拖慢软件运行速度
> 2. 日志文件
>    * 日志文件有两种：
>     1. 软件目录下的`logs`文件夹下为MFAAvalonia的运行日志
>     2. 软件目录下的`debug`文件夹下为MaaFramework的运行日志
>    * 这两种日志文件不需要调整选项就会自动生成，且会在软件运行时不断记录
>    * 一般建议上传`debug`文件夹下的日志文件，详情请参见issue模板
### **新模块开发**
   * 如果你想对某个全新的游戏模块进行功能开发，且不会对现有模块结构进行破坏性修改，只要能够顺利运行，都会直接采纳并合并
   * 在开发之前，我们强烈建议你**通过PR或者Issue占位**，或者**直接对当下活跃的开发者们打声招呼**，避免撞车
### **对现有模块进行功能追加**
   * 爬塔之外的模块，由于代码量较小，只要在**对代码有理解的基础下**进行追加（非无脑AI编程），不限制形式，能顺利运行都会采纳并合并
   * 针对爬塔模块，由于代码量较大，直接注入式追加可能会对后续维护产生困难，当前正在进行代码架构重构，如要进行功能追加，请先与维护者讨论方案后再进行
### **代码重构**
   * 我们知道能实现该需求的，一定是对MaaFramework有一定了解，且有一定编程能力的开发者
   * 但我们强烈建议你在与当前维护者进行讨论后再决定，以免方向出现不一致
### **不同服务器的内容适配**
   * 星塔助手虽然支持不同地区服务器的使用，但由于开发及测试人手的不足，无法做到所有服务器的体验都一致
   * 当前国服日服适配较好，台服国际服部分适配，韩服0适配
   * 如果你愿意协助提升不同地区服务器的使用质量，可以与维护者联系，或自查当前代码中缺失或错误的部分进行修改并提交PR
### **其他**
   * 对于上面没列出的部分，可以先提交issue或者直接联系当前维护者进行讨论

没时间了，后面有空再写（跑